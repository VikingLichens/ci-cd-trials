name: CI-CD André Trials

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Chuck Norris is talking
        id: chuck_step
        uses: Satak/webrequest-action@v1.2.3
        with:
          url: https://api.chucknorris.io/jokes/random
          method: GET

      - name: Display Chuck Norris joke content
        run: |
          echo "Status code: ${{ steps.chuck_step.outputs.statusCode }}"
          echo '${{ steps.chuck_step.outputs.output }}' | jq -r '.data.value'

      - name: Install dependencies
        run: bun install

      - name: Run unit tests
        run: bun test
