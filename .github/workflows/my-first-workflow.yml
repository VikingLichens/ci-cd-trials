name: CI-CD André Trials

on:
  push:
    branches: [ "main" ]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Chuck Norris is talking
        uses: Satak/webrequest-action@v1.2.3
        with:
          url: https://api.chucknorris.io/jokes/random
          method: GET

      - name: Display Chuck's joke
        run: |
          echo "Status code: ${{ steps.webhook.outputs.statusCode }}"
          echo '${{ steps.webhook.outputs.output }}' | jq -r '.data.value'          

      - name: install dependencies
        run: |
          echo installing dependencies using Bun
          bun install

      - name: running unit tests
        run: |
          echo Running unit tests using Vitest
          bun test
